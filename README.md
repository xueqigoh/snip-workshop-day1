# Snip Backend

A zero-dependency Bun URL shortener. Links are stored in memory and reset when the server restarts.

## Run

```sh
bun start
```

Set `PORT`, `BASE_URL`, `RAILWAY_PUBLIC_DOMAIN`, or `PUBLIC_DIR` to configure the server. The API listens on port 3000 by default.
